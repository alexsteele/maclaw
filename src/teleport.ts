/**
 * Public teleport module facade.
 *
 * Teleport internals now live under `src/teleport/`. This file keeps the
 * historical `./teleport.js` import path stable for the rest of the codebase.
 */
export * from "./teleport/index.js";
